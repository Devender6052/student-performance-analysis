# set is the collection of unsorted items
# each element in the set must be unique and immutable

some ={11,2,4,"hello","world","world"}
print(some)
print(type(some))
print(len(some)) # only count unique value and ignore duplicate values
